query FindConnectionGraph($root: String!, $folderId: String!) {
  findConnectionGraph(root: $root, folderId: $folderId) {
    root
    connections {
      id
      from
      to
      unknown
      type
      k162
      eol
      eolAt
      massStatus
      reverse
      depth
    }
  }
}
