query FindConnectionGraph($root: String!) {
  findConnectionGraph(root: $root) {
    root
    connections {
      id
      from
      to
      unknown
      type
      k162
      eol
      eolAt
      massStatus
      reverse
      depth
    }
  }
}
