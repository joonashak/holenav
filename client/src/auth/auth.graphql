mutation GetToken($state: String!) {
  getToken(state: $state) {
    accessToken
  }
}

query StartSsoLogin {
  startSsoLogin {
    ssoLoginUrl
  }
}

query AddCharacter {
  addCharacter {
    ssoLoginUrl
  }
}
